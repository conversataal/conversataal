language: node_js
node_js:
- stable
script: npm run dist
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  target_branch: master
  on:
    branch: src
    local_dir: dist
